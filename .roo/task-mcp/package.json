{
  "name": "task-mcp-server",
  "version": "1.0.0",
  "description": "MCP Server for task management",
  "main": "build/task_server.js",
  "scripts": {
    "start": "node build/task_server.js",
    "build": "rm -rf build && mkdir build && cp task_server.js build/task_server.js && chmod +x build/task_server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mcp",
    "task-manager"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@modelcontextprotocol/sdk": "*"
  }
}