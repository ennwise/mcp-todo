{
  "name": "My Dev Container",
  // Add other configurations like image, Dockerfile, extensions, settings, etc.
  // For example, if your Dockerfile is in the root:
  // "build": {
  //  "dockerfile": "../Dockerfile" // Or "Dockerfile" if devcontainer.json is in the root
  // },

  // Mount the Docker socket
  "mounts": [
    "source=/var/run/docker.sock,target=/var/run/docker.sock,type=bind"
  ],

  // Example: Forward a port from the container to the host
  // "forwardPorts": [3000],

  // Example: Install VS Code extensions in the container
  // "customizations": {
  //  "vscode": {
  //    "extensions": [
  //      "rust-lang.rust-analyzer"
  //    ]
  //  }
  // }

  // Keep the container running when VS Code closes
  // "shutdownAction": "none"
}